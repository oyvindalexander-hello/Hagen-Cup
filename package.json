{
  "name": "hagen-cup",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --format=esm --jsx=automatic --outfile=public/app.js",
    "start": "node server.js",
    "dev": "npm run build && node server.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
